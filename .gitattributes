# I media di immobili/ e posts/ non stanno in git: la sorgente e il bucket
# S3 alorenzi-eu-media, popolato da `make sync-media` e servito via Imgix.
# Vedi .gitignore.
#
# Restano in LFS solo gli asset di identita del sito, stabili nel tempo.
media/avatar-profile.jpg filter=lfs diff=lfs merge=lfs -text
media/banner.jpg filter=lfs diff=lfs merge=lfs -text
